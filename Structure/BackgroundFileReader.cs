using System;
using System.Collections.Generic;
using System.Text;

namespace Structure.Structure
{
    class BackgroundFileReader
    {
        private Dictionary<string, string> _readFiles = new Dictionary<string, string>();

        //public string ReadFile(string key)
        //{
        //    if (_readFiles.TryGetValue(key, out var value)) return value;
        //    else (ReadFile(key, value));
        //}
    }
}
